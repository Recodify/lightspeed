Currently the length of the variant workload run is determined by duration in seconds. It would perhaps be
useful to have the ability to instead specify the number of queries executions to run and then the result
becomes time to complete x queries rather than qps.