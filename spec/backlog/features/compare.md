### Output

Currently we dump raw data but it's a bit of a mission to read. It would be great if we could generate a plan text summart of the results.

There are a couple of options for this:
    - alogrithmically
    - calling a frontier llm if they user supplies an api key
    - supporting a local llm and providing the tools for standing up a docker container/oolama/pull model etc